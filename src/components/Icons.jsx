/*
import React from 'react'
import performanceImages from 'src/constants/index.js';

const Icons = () => {
    return (
        <div className="wrapper">
            {performanceImages.map((item, index ) => (
                <img
                    key={index}
                    src={item.src}
                    className={item.id}
                    alt={item.alt || `Performance Image #${index + 1}`}
                />
            ))}
        </div>
    )
}
export default Icons
*/
